[project]
name = "cc-challenge-backend"
version = "0.1.0"
description = "FastAPI starter for the sewage pipe project coding challenge"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.116.0",
    "psycopg[binary,pool]>=3.2.9",
    "pydantic-settings>=2.10.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
]

